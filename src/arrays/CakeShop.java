package arrays;

import java.util.Arrays;
import java.util.stream.IntStream;

public class CakeShop {

//    public boolean servedCorrect(int[] take_out, int[] dine_in, int[] served) {
//        if(served.length == 0) {
//            return true;
//        }
//
//        //if first order is from take_out
//        if(take_out.length > 0 && take_out[0] == served[0]) {
//            int[] subArray = IntStream.range(1, take_out.length).mapToObj(i -> take_out[i]).toArray(Integer[]::new);
//            return servedCorrect(Arrays.copyOf(take_out, 1, take_out.length));
//        }
//    }
}
